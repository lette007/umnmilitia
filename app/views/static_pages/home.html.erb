<div class="center jumbotron">
  <% provide(:title, "home") %>
  <% if logged_in? %>
    <ul class="users">
      <%= render @users.current_user %>
    </ul>
  <% else %>
    <%= link_to "LOGIN", login_path, class: "btn btn-lg btn-primary" %>
    <%= link_to "ENLIST", signup_path, class: "btn btn-lg btn-primary" %>
  <% end %>
  
</div>

